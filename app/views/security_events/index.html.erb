<%= page_heading 'My security events' %>

<%= render partial: 'table',
           locals: {
             security_events: @security_events,
             next_page: @next_page,
             prev_page: @prev_page,
           } %>
