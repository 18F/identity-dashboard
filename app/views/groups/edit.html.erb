<!-- 
Karla Rodriguez
added title from style guide
-->
<div class="usa-display">Edit group</div>


<%= simple_form_for(@group, html: { autocomplete: 'off', role: 'form' }) do |form| %>
  <%= form.error_notification %>
  <%= render 'form', form: form %>
  <%= form.button :submit, 'Update' %>
<% end %>
