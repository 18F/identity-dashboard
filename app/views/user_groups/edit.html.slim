h2 = t('headings.user_groups.edit')

= simple_form_for(@user_group, html: { autocomplete: 'off', role: 'form' }) do |form|
  = form.error_notification
  = render 'form', form: form
  = form.button :submit, 'Update'
