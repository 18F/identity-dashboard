h2 = t('headings.user_groups.new')

= simple_form_for(@user_group,
  html: { autocomplete: 'off', role: 'form' },
  url: user_groups_path) do |form|
  = form.error_notification
  = render 'form', form: form
  = form.button :submit, 'Create'
