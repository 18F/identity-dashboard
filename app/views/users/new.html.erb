<%= page_heading 'New user' %>

<%= simple_form_for(@user,
    html: { autocomplete: 'off' },
    url: users_path) do |form| %>
  <%= render 'form', form: form %>
  <%= form.button :submit, 'Create', :class => "usa-button margin-top-3" %>
<% end %>
