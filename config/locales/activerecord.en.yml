---
en:
  activerecord:
    errors:
      models:
        service_provider:
          attributes:
            issuer:
              invalid: is not formatted correctly
            saml_client_cert:
              invalid: is not valid
