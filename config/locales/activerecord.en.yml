---
en:
  activerecord:
    errors:
      models:
        service_provider:
          attributes:
            issuer:
              invalid: is not formatted correctly. The issuer must be a unique string with no spaces.
            certs:
              invalid: are not all valid
